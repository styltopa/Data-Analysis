Cheatsheet commands 

................ Basic commands to check for usage: ................
help <command-name>
doc <command-name>

- Remove nan values
rmmissing(A) - removes missing values of A (NaN, undefined etc)

................ Plots ................

- figure
bar (προσοχή το bar graph παίρνει τις συχνότητες εμφάνισης και όχι τις κανονικές τιμές όπως παίρνει το ιστόγραμμα). Ωστόσο, μπορείς να χρησιμοποιήσεις και το histogram για να φτιάξεις bar graph και ακόμη ακόμη να βάλεις κατηγορικά δεδομένα στον οριζόντιο άξονα.

Για εμφάνιση δεδομένων ως plot με κατηγορικά δεδομένα.
X = categorical(v, vSet, cellstr(num2str(unique(v, 'sorted'))));
histogram(X, 'BarWidth', 0.5);


histogram 
scatter

title
legend
xabel
ylabel
annotation (για boxes μέσα στο plot)
xline
yline
xlim
ylim

hold on
hold off


clf
close all

................ Tests ................ 
ttest
ttest2

norminv	

vartest
chi2gof

corr - για το συντελεστή συσχέτισης 
corrcoef - για όλο το πίνακα συσχέτισης (περιέχει και τους άσσους στη διαγώνιο)
(corr = corrcoef(1, 2) = corrcoef(2, 1) για τον πίνακα συσχέτισης μεταξύ δύο μεταβλητών Χ, Υ)

bootstrap
bootci

bootSamCorrCoef = bootci(B, {@corr, yearsAndPPNotNan(:, 1), ...
    yearsAndPPNotNan(:, 2)}, 'Alpha', alpha);

regress
stepwisefit (;)

Εύρεση διαστήματος εμπιστοσύνης κανονικής κατανομής

................ Other commands ................
unique - μοναδικές τιμές 
- Function handlers: capable = @(x)(USL-LSL)./(6*std(x));
[x, y] = deal(arr1, arr2);

length - για την μεγαλύτερη διάσταση του πίνακα.
size - για όλες τις διαστάσεις του πίνακα.

................ Ανάγνωση δεδομένων ................ 
readtable (επιστρέφει δεδομένα πίνακα)
table2array
importdata (επιστρέφει τα δεδομένα σε struct)
Τότε μπορείς να τα καλέσεις με τελεία και το αντίστοιχο field.



................ Αντιστοίχιση ασκήσεων σε θέματα ................ 
2.1
2.2
2.3
2.4
2.5
2.6
2.7

3.1
3.2
3.3
3.4
3.5
3.6
3.7

4.1
4.2
4.3
4.4

5.1
5.2
5.3
...
5.11

6.1
6.2
...

(δες από Κουγιουμτζή αυτές)